import type {
  OAuthAuthorizationPrincipal,
  OAuthIdentityAccessToken,
} from "./principal-store.js";

export type VerifiedOAuthAccessToken = OAuthIdentityAccessToken;
export type OAuthPrincipal = OAuthAuthorizationPrincipal;
